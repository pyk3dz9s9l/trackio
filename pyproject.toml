[build-system]
requires = ["setuptools>=68"]
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "trackio"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = []
spaces = []

[tool.setuptools]
packages = ["trackio"]
