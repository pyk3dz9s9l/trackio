[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "trackio"
version = "0.0.0"
description = "minimal verification build"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = []
spaces = []

[tool.setuptools]
packages = []
